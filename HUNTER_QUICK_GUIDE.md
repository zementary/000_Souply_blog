# 🚀 Hunter v2.0 快速使用指南

**版本**: 2.0 (多年份支持)  
**更新**: 2026-01-17

---

## 📖 三种使用方式

### 1️⃣ 自动扫描所有年份（推荐）

```bash
npm run hunter
```

**效果**：
- ✅ 自动发现 `src/data/` 下的所有 `YYYY.csv` 文件
- ✅ 按时间顺序处理（2015 → 2016 → 2024）
- ✅ 忽略非年份文件（如 `test-cleaning.csv`）
- ✅ 显示每个文件的摘要 + 总摘要

**适用场景**：
- 初次导入所有历史数据
- 批量更新多个年份
- 确保所有数据都已录入

**示例输出**：
```
╔════════════════════════════════════════╗
║  SOUPLY HUNTER v2.0                    ║
║  Multi-Year CSV Batch Processor        ║
╚════════════════════════════════════════╝

📄 Mode: Auto-scan (found 3 files)

   - 2015.csv
   - 2016.csv
   - 2024.csv

════════════════════════════════════════════════════════════
📂 Processing File 1/3: 2015.csv
════════════════════════════════════════════════════════════
...
```

---

### 2️⃣ 处理指定年份

```bash
npm run hunter 2015
```

**效果**：
- ✅ 只处理 `src/data/2015.csv`
- ✅ 快速聚焦单个年份
- ✅ 适合增量更新

**适用场景**：
- 添加或更新某一年的数据
- 测试新数据
- 修复特定年份的问题

**示例输出**：
```
╔════════════════════════════════════════╗
║  SOUPLY HUNTER v2.0                    ║
║  Multi-Year CSV Batch Processor        ║
╚════════════════════════════════════════╝

📄 Mode: Single year (2015)

════════════════════════════════════════════════════════════
📂 Processing File 1/1: 2015.csv
════════════════════════════════════════════════════════════
...
```

---

### 3️⃣ 使用自定义文件

```bash
npm run hunter -- --file src/data/test-cleaning.csv
```

**效果**：
- ✅ 处理任意 CSV 文件（不限于年份格式）
- ✅ 适合测试和特殊场景

**适用场景**：
- 测试数据
- 特殊播放列表
- 临时导入

---

## ⚠️ 常见问题

### Q1: 找不到年份文件

```bash
$ npm run hunter 2017

❌ Error: CSV file for year 2017 not found
   Expected location: src/data/2017.csv

Available years:
   - 2015
   - 2016
   - 2024
```

**解决**：确保文件存在且命名格式为 `YYYY.csv`（4 位数字年份）

---

### Q2: 脚本一直处理已存在的视频

**正常行为**！脚本会自动跳过已录入的视频：

```
[1/23] ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📼 Processing: Charli XCX - 360
...
   ⏭  Skipping: Already exists in 2024-charli-xcx-360.mdx
```

这是**幂等性设计**，可以安全地重复运行。

---

### Q3: 如何并行处理多个年份？

打开多个终端窗口：

```bash
# Terminal 1
npm run hunter 2015

# Terminal 2
npm run hunter 2016

# Terminal 3
npm run hunter 2024
```

⚠️ **注意**：确保不同终端处理不同的文件，避免冲突。

---

## 📊 性能参考

| 文件 | 视频数 | 预估时间 | 说明 |
|------|-------|---------|------|
| 2015.csv | 20 | ~60 秒 | 3秒/视频 × 20 |
| 2016.csv | 1 | ~3 秒 | 测试文件 |
| 2024.csv | 23 | ~69 秒 | 3秒/视频 × 23 |
| **全部** | **44** | **~2.5 分钟** | 含文件间 5秒 暂停 |

---

## 🎯 推荐工作流

### 首次使用（初始化所有数据）

```bash
# 1. 自动处理所有年份
npm run hunter

# 2. 检查数据质量
npm run check-quality

# 3. 手动修复问题（如果有）
# 参考 START_HERE.md
```

---

### 日常使用（添加新年份）

```bash
# 1. 创建新文件：src/data/2025.csv
# 2. 填写数据
# 3. 处理新年份
npm run hunter 2025

# 4. 检查质量
npm run check-quality

# 5. 填写 curator_note
```

---

### 测试使用（验证数据）

```bash
# 1. 创建测试文件：src/data/test.csv
# 2. 添加几条测试数据
# 3. 处理测试文件
npm run hunter -- --file src/data/test.csv

# 4. 验证结果
ls src/content/videos/ | grep test

# 5. 清理测试数据
rm src/content/videos/*test*.mdx
```

---

## 📋 文件命名规范

### ✅ 会被自动识别

- `2015.csv` - 标准年份
- `2024.csv` - 标准年份
- `2099.csv` - 未来年份（也有效）

### ❌ 不会被自动识别

- `test-cleaning.csv` - 不是年份格式
- `2024-backup.csv` - 有额外后缀
- `backup-2024.csv` - 有额外前缀
- `24.csv` - 年份不是 4 位数

**使用方法**：通过 `--file` 参数手动指定

---

## 🛠️ 故障排除

### 脚本执行卡住

**原因**：可能在等待 YouTube API 响应

**解决**：
1. 等待几秒（每个视频 3 秒 + YouTube 搜索时间）
2. 检查网络连接
3. 使用 Ctrl+C 中断，然后重新运行（会跳过已处理的视频）

---

### 搜索失败

```
⚠ Skipping: Could not find video on YouTube
```

**原因**：
- YouTube 搜索未找到匹配视频
- 艺术家/标题拼写错误
- 视频已被删除

**解决**：
1. 检查 CSV 中的艺术家/标题拼写
2. 手动在 YouTube 搜索确认视频存在
3. 使用 `npm run ingest <url>` 手动录入

---

### Junk 过滤

```
🚫 Skipping Junk Video: "Best of 2015 Music Videos"
```

**原因**：自动过滤了非官方视频（合集、预告片等）

**解决**：
1. 检查是否误判（查看 `scripts/hunter.js` 第 81-112 行的 BLOCKLIST）
2. 如果确实是官方视频，修改 CSV 中的标题
3. 或调整 BLOCKLIST

---

## 📚 相关文档

| 文档 | 用途 |
|------|------|
| `HUNTER_QUICK_GUIDE.md` | **本指南**（快速参考）|
| `HUNTER_V2.0_CHANGELOG.md` | 版本更新日志 |
| `HUNTER_WORKFLOW.md` | 详细工作流程 |
| `INGEST_QUICKSTART.md` | 单视频录入指南 |
| `START_HERE.md` | 数据质量检查 |

---

## 🎉 快速命令参考

```bash
# 自动处理所有年份
npm run hunter

# 处理特定年份
npm run hunter 2015
npm run hunter 2024

# 处理自定义文件
npm run hunter -- --file src/data/test.csv

# 查看可用年份
ls src/data/*.csv

# 检查数据质量
npm run check-quality
```

---

**Happy Hunting!** 🏹✨
